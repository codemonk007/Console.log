// const addAndPrint = (a, b) => { 
//     const sum = a+b;
//     // console.log(`The sum is ${sum}`);
//     return sum;
//   };
//   const ans = addAndPrint(4,5)
const addAndPrint = (a, b) => { 
    const sum = a+b;
    return sum;
  };
  const ans = addAndPrint(4,5)
  console.log("The sum is " + ans)